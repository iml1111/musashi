import React from 'react'
import { Handle, Position } from 'reactflow'

// Simple test node
const SimpleNode = ({ data }: any) => {
  return (
    <div style={{
      padding: '10px',
      border: '1px solid #ccc',
      borderRadius: '5px',
      background: '#fff'
    }}>
      <Handle type="target" position={Position.Top} />
      <div>{data.label || 'Node'}</div>
      <Handle type="source" position={Position.Bottom} />
    </div>
  )
}

export const simpleNodeTypes = {
  userinput: SimpleNode,
  agent: SimpleNode,
  agenttool: SimpleNode,
  vectorstore: SimpleNode,
  knowledgebase: SimpleNode,
  mcp: SimpleNode,
  apicall: SimpleNode,
  router: SimpleNode,
  finaloutput: SimpleNode,
  default: SimpleNode,
}